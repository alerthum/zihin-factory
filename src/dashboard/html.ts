export function dashboardHtml(): string {
  return `<!doctype html>
<html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Zihin Factory — Komuta Merkezi</title>
<style>
:root{color-scheme:dark;--bg:#0b0d10;--card:#15181d;--card2:#101318;--line:#2a3038;--txt:#f5f7fa;--muted:#98a2b3;--orange:#ff7a18;--green:#29c36a;--red:#ff5d5d;--yellow:#f6c453;--blue:#62a8ff;--violet:#aa7dff}
*{box-sizing:border-box}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:radial-gradient(circle at 20% -10%,#2a1608 0,transparent 32%),var(--bg);color:var(--txt)}button,input{font:inherit}.wrap{max-width:1520px;margin:auto;padding:22px}.top{display:flex;align-items:center;gap:16px;justify-content:space-between;position:sticky;top:0;z-index:4;background:rgba(11,13,16,.94);backdrop-filter:blur(14px);padding:14px 0;border-bottom:1px solid var(--line)}.brand{display:flex;gap:12px;align-items:center}.logo{width:42px;height:42px;border-radius:12px;background:linear-gradient(135deg,var(--orange),#ff3d00);display:grid;place-items:center;font-weight:900;font-size:21px}.brand h1{font-size:18px;margin:0}.brand small,.muted{color:var(--muted)}.actions{display:flex;gap:8px;flex-wrap:wrap}.btn{border:1px solid var(--line);background:#171a1f;color:var(--txt);padding:9px 12px;border-radius:10px;cursor:pointer}.btn:hover{border-color:#56606d}.btn.primary{background:var(--orange);border-color:var(--orange);color:#111;font-weight:850}.btn.danger{border-color:#6a3434;color:#ffb0b0}.btn:disabled{opacity:.45;cursor:not-allowed}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:14px;margin-top:16px}.card{background:linear-gradient(180deg,#171a1f,#121519);border:1px solid var(--line);border-radius:15px;padding:15px;min-width:0}.span3{grid-column:span 3}.span4{grid-column:span 4}.span6{grid-column:span 6}.span8{grid-column:span 8}.span12{grid-column:span 12}.card h2{font-size:14px;margin:0 0 12px}.big{font-size:30px;font-weight:850;letter-spacing:-1px}.status{display:inline-flex;gap:6px;align-items:center;border:1px solid var(--line);border-radius:999px;padding:4px 8px;font-size:11px}.dot{width:7px;height:7px;border-radius:50%;background:var(--muted)}.ok .dot{background:var(--green);box-shadow:0 0 8px var(--green)}.warn .dot{background:var(--yellow)}.bad .dot{background:var(--red)}.bar{height:7px;background:#252a31;border-radius:99px;overflow:hidden;margin-top:10px}.bar>i{height:100%;display:block;background:linear-gradient(90deg,var(--orange),#ff9a3d);border-radius:99px}.tabs{display:flex;gap:8px;overflow:auto;padding:2px 0 8px}.tab{white-space:nowrap;background:#14171c;border:1px solid var(--line);color:var(--txt);padding:9px 12px;border-radius:10px;cursor:pointer}.tab.on{border-color:var(--orange);color:#fff3e9;background:#2b1608}.panel{display:none}.panel.on{display:block}.agent-grid,.lane-grid,.help-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.agent,.lane,.help,.impact,.issue{border:1px solid var(--line);background:var(--card2);border-radius:12px;padding:12px}.agent strong,.help strong{display:block;margin-bottom:7px}.agent .task{font-size:13px;margin-top:8px;min-height:34px}.meta{font-size:11px;color:#8eb6df;margin-top:10px}.desc{font-size:12px;color:#b7c0cc;line-height:1.45;margin-top:8px}.lane strong{display:block}.lane .count{font-size:24px;font-weight:850;margin-top:6px}.issue{border-left:4px solid var(--yellow);margin-bottom:10px}.issue.error{border-left-color:var(--red)}.issue.info{border-left-color:var(--blue)}.issue h3{margin:0 0 8px;font-size:14px}.issue p{margin:5px 0;font-size:13px;line-height:1.45}.recommend{margin-top:9px;padding:9px;border-radius:9px;background:#0d1720;border:1px solid #263b50}.technical{margin-top:8px;color:var(--muted);font-size:11px;word-break:break-word}details summary{cursor:pointer;color:#b8c6d7}table{width:100%;border-collapse:collapse;font-size:12px}th,td{text-align:left;padding:10px 8px;border-bottom:1px solid #252a31;vertical-align:top}th{color:#91a4bd;font-weight:600}.pill{display:inline-block;border:1px solid var(--line);border-radius:999px;padding:3px 7px;font-size:10px}.pill.done,.pill.completed,.pill.merged,.pill.PASS{border-color:#246b43;color:#83e9a9}.pill.blocked,.pill.failed,.pill.error{border-color:#7c3434;color:#ff9999}.pill.quarantine,.pill.RETRY,.pill.warning{border-color:#7d651b;color:#ffd86c}.pill.dispatched,.pill.running,.pill.verify,.pill.waiting-ci,.pill.waiting-human{border-color:#2b5f91;color:#8fc5ff}.timeline .event{display:grid;grid-template-columns:155px 1fr;gap:12px;padding:10px 0;border-bottom:1px solid #252a31}.event time{color:#8aa5c3;font-size:11px}.empty{text-align:center;color:#8ca1b8;padding:30px}.score-help{font-size:11px;color:#b6c2d0;line-height:1.35}.toast{position:fixed;right:20px;bottom:20px;background:#1d2229;border:1px solid #3b4653;padding:12px 14px;border-radius:10px;display:none;z-index:10}.login{position:fixed;inset:0;background:#090b0ee8;display:grid;place-items:center;z-index:20}.loginbox{width:min(430px,92vw);background:#15181d;border:1px solid var(--line);border-radius:16px;padding:22px}.login input{width:100%;padding:12px;border-radius:10px;background:#0c0f13;color:white;border:1px solid var(--line);margin:10px 0}.command-note{font-size:11px;color:#9fb0c2;margin-top:5px}.qa-note{padding:12px;border:1px solid #3b4653;background:#111820;border-radius:11px;margin-bottom:12px;color:#c8d5e3;font-size:12px;line-height:1.5}
@media(max-width:980px){.span3,.span4,.span6,.span8{grid-column:span 12}.agent-grid,.lane-grid,.help-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.top{align-items:flex-start;flex-direction:column}.timeline .event{grid-template-columns:1fr}}
@media(max-width:600px){.wrap{padding:12px}.agent-grid,.lane-grid,.help-grid{grid-template-columns:1fr}.actions .btn{font-size:12px;padding:8px}.big{font-size:26px}}
</style></head><body>
<div id="login" class="login"><div class="loginbox"><h2>Zihin Factory Komuta Merkezi</h2><div class="muted">FACTORY_ADMIN_TOKEN yalnız bu tarayıcı oturumunda saklanır.</div><input id="token" type="password" placeholder="FACTORY_ADMIN_TOKEN"><button id="connect" class="btn primary">Bağlan</button></div></div>
<div class="wrap"><header class="top"><div class="brand"><div class="logo">ZF</div><div><h1>Zihin Factory — Komuta Merkezi</h1><small id="subtitle">Dashboard bağlanıyor…</small></div></div><div class="actions">
<button class="btn primary" data-action="start" title="Fabrika duraklatılmışsa yeniden çalıştırır. Normal çalışma sırasında kullanmanız gerekmez.">▶ Devam Ettir</button>
<button class="btn" data-action="cycle" title="Dakikalık cron'u beklemeden Governor planlayıcısını bir kez hemen çalıştırır. Normalde otomatik çalışır.">↻ Planlayıcıyı Şimdi Çalıştır</button>
<button class="btn" data-action="feed" title="Zihin Arenası canlı reposunu ve proje roadmap'ini şimdi besleme turuna sokar. Normalde otomatik çalışır.">＋ Projeyi Besle</button>
<button class="btn" data-action="recover" title="Heartbeat'i kesilmiş gerçekten takılmış Workflow'ları kurtarır. Yalnız dashboard STALLED gösterirse kullanın.">⚕ Takılan İşi Kurtar</button>
<button class="btn danger" data-action="pause" title="Yeni iş başlatmayı durdurur. Mevcut çalışan işi zorla öldürmez.">Ⅱ Fabrikayı Duraklat</button>
<button class="btn" id="logout">Token Çıkış</button></div></header>
<section class="grid"><div class="card span3"><h2>CANLI MOTOR ÇALIŞMASI</h2><div id="motor" class="big">-</div><div id="motorDetail" class="status"><i class="dot"></i>-</div></div><div class="card span3"><h2>ZİHİN ARENASI</h2><div id="completion" class="big">82%</div><div class="muted">raporlanan genel ürün baseline</div><div class="bar"><i id="completionBar" style="width:82%"></i></div></div><div class="card span3"><h2>AKTİF İŞ</h2><div id="activeCount" class="big">0 / 4</div><div id="waitingHuman" class="muted">insan merge bekleyen: 0</div></div><div class="card span3"><h2>BUGÜNKÜ ETKİ</h2><div id="todayImpact" class="big">0</div><div class="muted">tamamlanan/merge edilen proje etkisi</div></div></section>
<section class="grid"><div class="card span12"><div class="tabs" id="tabs"><button class="tab on" data-tab="overview">Genel Bakış</button><button class="tab" data-tab="agents">Müdürler / Ajanlar</button><button class="tab" data-tab="roadmap">Canlı İş Akışı</button><button class="tab" data-tab="impact">Zihin Arenası Etkisi</button><button class="tab" data-tab="github">GitHub / Taslak PR</button><button class="tab" data-tab="qa">Kalite Kontrol</button><button class="tab" data-tab="learning">Fabrika Öğrenmesi</button><button class="tab" data-tab="events">Canlı Olaylar</button><button class="tab" data-tab="blockers">Sorunlar / Çözümler</button></div>
<div id="overview" class="panel on"></div><div id="agents" class="panel"></div><div id="roadmap" class="panel"></div><div id="impact" class="panel"></div><div id="github" class="panel"></div><div id="qa" class="panel"></div><div id="learning" class="panel"></div><div id="events" class="panel"></div><div id="blockers" class="panel"></div></div></section></div><div id="toast" class="toast"></div>
<script>
const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const arr=v=>Array.isArray(v)?v:[];let data=null,timer=null,token=sessionStorage.getItem('zf_admin_token')||'';
const roleInfo={
'Research Scout':['Araştırma Tarama Müdürü','Güncel araştırma ve kanıt alanlarını bulur; neyin araştırılması gerektiğini çıkarır.'],
'Source Auditor':['Kaynak Doğrulama Müdürü','Kullanılan kaynakların doğruluğunu, güvenilirliğini ve kanıt değerini denetler.'],
'Structure Miner':['Yapı Haritalama Müdürü','Canlı repository yapısını, gerçek dosyaları ve motor sözleşmelerini haritalar.'],
'Curriculum Mapper':['Müfredat Eşleme Müdürü','Kazanım, sınıf ve içerik hedeflerini soru motoru yapılarıyla eşler.'],
'Factory Designer':['Soru Fabrikası Tasarım Müdürü','ECD + AIG item factory sözleşmelerini ve üretim kurallarını tasarlar.'],
'Generator':['Soru Üretim Müdürü','Parametrik soru üretimini, varyasyonları ve içerik üretim akışını yürütür.'],
'Solver':['Çözüm / Doğru Cevap Müdürü','Sorunun oracle/solver doğruluğunu ve deterministik doğru cevabını üretir.'],
'Distractor Engineer':['Çeldirici Müdürü','Yanlış seçenekleri gerçek öğrenci yanılgılarına göre tasarlar ve denetler.'],
'Tutor Designer':['Çözüm ve İpucu Müdürü','Kademeli ipuçlarını ve öğretici adım adım çözümü tasarlar.'],
'Child Reviewer':['Öğrenci Gözüyle İnceleme Müdürü','Dil, yaş düzeyi, anlaşılabilirlik ve öğrenci deneyimi açısından içeriği inceler.'],
'Fairness Reviewer':['Adalet / Erişilebilirlik Müdürü','Önyargı, adalet, kapsayıcılık ve erişilebilirlik risklerini denetler.'],
'IP/Security Reviewer':['Telif / Güvenlik Müdürü','Telif, gizli bilgi, secret ve güvenlik risklerini kontrol eder.'],
'Psychometrician':['Ölçme Değerlendirme Müdürü','Zorluk ve ölçme kalitesini değerlendirir; veri yoksa sahte psikometri üretmez.'],
'Game Planner':['Oyun Uyum Müdürü','Canonical item modelini oyunların sunum/evidence adapter katmanına eşler.'],
'Codex Engineer':['Kod Geliştirme Müdürü','Canlı repo kaynaklarını okuyup küçük, geri alınabilir kod/test patchleri hazırlar.'],
'QA Supervisor':['Bağımsız Kalite Müdürü','Üreten ajandan bağımsız olarak kabul kriterleri, kalite ve güvenlik kapısını uygular.'],
'Release Manager':['Yayın Müdürü','PR, CI, merge ve yayın kapılarını denetler; fail-closed karar verir.']};
const statusTr={ready:'hazır',dispatched:'gönderildi',running:'çalışıyor',verify:'doğrulanıyor',done:'tamamlandı',completed:'tamamlandı',failed:'başarısız',blocked:'engellendi',quarantine:'karantinada','waiting-ci':'gerçek CI bekleniyor','waiting-human':'merge onayı bekliyor',merged:'merge edildi',abandoned:'terk edildi',PASS:'GEÇTİ',RETRY:'REVİZYON',BLOCKED:'ENGELLENDİ',QUARANTINE:'KARANTİNA'};
const eventTr={workflow_started:'İş akışı başladı',producer_started:'Üretim başladı',producer_completed:'İlk üretim tamamlandı',producer_revision_started:'Revizyon başladı',producer_revision_completed:'Revizyon tamamlandı',qa_started:'Bağımsız kalite kontrol başladı',qa_completed:'Bağımsız kalite kontrol tamamlandı',qa_revision_started:'Bağımsız kalite revizyon kontrolü başladı',qa_revision_completed:'Bağımsız kalite revizyon kontrolü tamamlandı',repo_recon_evidence_ready:'Canlı repo kanıtı hazır',workflow_failed:'İş akışı hatası',provider_retry_scheduled:'Otomatik sağlayıcı tekrarı planlandı',product_patch_discovery_started:'Kod patch için repo incelemesi başladı',product_patch_producer_started:'Kod değişikliği üretimi başladı',product_patch_qa_started:'Kod değişikliği bağımsız kalite kontrolünde',product_patch_qa_completed:'Kod değişikliği kalite kontrolü tamamlandı',product_patch_revision_started:'Kod değişikliği revizyonu başladı',product_patch_json_repair_started:'Kod patch formatı otomatik düzeltiliyor',product_patch_json_repair_completed:'Kod patch formatı otomatik düzeltildi',product_patch_github_started:'GitHub Taslak PR hazırlığı başladı',product_draft_pr_created:'Zihin Arenası Taslak PR oluşturuldu'};
function state(k){return data?.stateMap?.[k]??null}function pill(v){const raw=String(v??'-');return '<span class="pill '+esc(raw)+'">'+esc(statusTr[raw]||raw)+'</span>'}function fmt(t){if(!t)return'-';const s=String(t);let d=new Date(s);if(!Number.isFinite(d.getTime())&&!s.includes('T'))d=new Date(s.replace(' ','T')+'Z');return Number.isFinite(d.getTime())?d.toLocaleString('tr-TR'):'-'}
function friendlyEvent(e){let msg=String(e.message||'');msg=msg.replace(/Independent QA attempt ([0-9]+) completed: (PASS|RETRY|QUARANTINE|BLOCKED) ([0-9]+)[/]100/i,'Bağımsız kalite kontrol $1. deneme tamamlandı: $2 $3/100').replace(/Independent QA attempt ([0-9]+) started/i,'Bağımsız kalite kontrol $1. deneme başladı').replace(/Producer revision ([0-9]+) completed/i,'Üretici revizyon $1 tamamlandı').replace(/Producer revision ([0-9]+) started/i,'Üretici revizyon $1 başladı').replace(/Producer attempt ([0-9]+) completed/i,'Üretici $1. deneme tamamlandı').replace(/Producer attempt ([0-9]+) started/i,'Üretici $1. deneme başladı').replace(/Live repo evidence loaded: ([0-9]+) candidate paths/i,'Canlı repository kanıtı yüklendi: $1 aday dosya yolu');return msg}
function qaMeaning(r){const s=Number(r.score||0),d=String(r.decision||'');if(d==='PASS')return 'Bu görevin çıktısı kabul kriterlerini geçti. Bu puan proje tamamlanma yüzdesi değildir.';if(d==='RETRY')return s>=70?'Çıktı önceki denemeye göre güçlü olabilir; fakat bazı kabul kriterleri hâlâ eksik olduğu için otomatik revizyon istendi.':'Önemli eksikler var; fabrika aynı görevi QA talimatıyla otomatik revize ediyor.';if(d==='QUARANTINE')return 'Revizyon bütçesi sonunda kalite yeterli olmadı; çıktı ürüne uygulanmadı ve güvenli karantinaya alındı.';if(d==='BLOCKED')return 'Kalite değil, dış veri/erişim/karar gibi bir engel nedeniyle görev ilerleyemedi.';return 'Bu puan yalnız bu görev çıktısının bağımsız kalite değerlendirmesidir.'}
function last6hSummary(){const x=data?.last6h||{};return 'Son 6 saat: '+Number(x.jobs||0)+' iş • '+Number(x.completed||0)+' tamamlandı • '+Number(x.quarantine||0)+' karantina • '+Number(x.blocked||0)+' engel • '+Number(x.prs||0)+' ürün PR'; }
function motorState(){const on=state('continuous_enabled')==='1',jobs=arr(data?.activeJobs),starting=arr(data?.startingJobs),eligible=arr(data?.eligibleRoadmap),blocked=arr(data?.blockedReadyRoadmap);if(!on)return{label:'DURAKLATILDI',cls:'warn',detail:'Yeni görev başlatma kapalı.'};if(jobs.length){let stale=false;for(const a of jobs){const d=new Date(String(a.updated_at||'').replace(' ','T')+'Z');if(Number.isFinite(d.getTime())&&(Date.now()-d.getTime())/60000>6)stale=true}return stale?{label:'TAKILMIŞ OLABİLİR',cls:'bad',detail:'En az bir iş 6 dakikadan uzun süredir heartbeat vermiyor.'}:{label:'ÇALIŞIYOR',cls:'ok',detail:jobs.length+' iş paralel çalışıyor.'}}if(starting.length)return{label:'BAŞLATILIYOR',cls:'ok',detail:starting.length+' iş Workflow başlangıç kuyruğunda.'};const retryAt=state('provider_retry_not_before');if(retryAt){const rd=new Date(retryAt);if(Number.isFinite(rd.getTime())&&rd.getTime()>Date.now())return{label:'SAĞLAYICI BEKLİYOR',cls:'warn',detail:'NVIDIA geçici hatası sonrası otomatik tekrar '+rd.toLocaleTimeString('tr-TR')+' zamanında yapılacak. Siz hiçbir şey yapmayın.'}}if(eligible.length)return{label:'BEKLİYOR',cls:'ok',detail:eligible.length+' uygun iş var; planlayıcı otomatik başlatacak. '+last6hSummary()};if(blocked.length)return{label:'BAĞIMLILIK BEKLİYOR',cls:'warn',detail:'Hazır görünen işler önceki görev/merge sonucunu bekliyor; adaptif besleyici bağımsız işler üretecek.'};return{label:'BEKLİYOR',cls:'ok',detail:'Proje Direktörü sürekli backlog dolduruyor. '+last6hSummary()}}
async function api(path,opt={}){const r=await fetch(path,{...opt,headers:{'authorization':'Bearer '+token,'content-type':'application/json',...(opt.headers||{})}});const j=await r.json();if(!r.ok)throw new Error(j.error||r.statusText);return j}function toast(t){const x=$('#toast');x.textContent=t;x.style.display='block';setTimeout(()=>x.style.display='none',3200)}function showLogin(){$('#login').style.display='grid'}function hideLogin(){$('#login').style.display='none'}
async function refresh(){try{data=await api('/dashboard/api');hideLogin();render()}catch(e){if(String(e.message).includes('unauthorized'))showLogin();else toast('Dashboard yenileme hatası: '+e.message)}}
function render(){const m=motorState();$('#motor').textContent=m.label;$('#motorDetail').className='status '+m.cls;$('#motorDetail').innerHTML='<i class="dot"></i>'+esc(m.detail);const pct=Number(state('project_completion_percent')||82);$('#completion').textContent=pct+'%';$('#completionBar').style.width=Math.max(0,Math.min(100,pct))+'%';$('#activeCount').textContent=(arr(data.activeJobs).length+arr(data.startingJobs).length)+' / '+Number(data.parallelLimit||4);$('#waitingHuman').textContent='insan merge bekleyen: '+Number(data.today?.waitingHuman||0);$('#todayImpact').textContent=Number(data.today?.completed||0)+Number(data.today?.merged||0);$('#subtitle').textContent='v'+esc(data.version)+' • '+m.label+' • son yenileme '+new Date().toLocaleTimeString('tr-TR');renderOverview();renderAgents();renderRoadmap();renderImpact();renderGitHub();renderQA();renderLearning();renderEvents();renderBlockers()}
function issueHtml(i){const g=i.guidance||{};return '<div class="issue '+esc(g.severity||'info')+'"><h3>'+esc(g.title||'Durum')+'</h3><p><b>Ne oldu?</b> '+esc(g.whatHappened||i.error_text||'')+'</p><p><b>Fabrika ne yapıyor?</b> '+esc(g.factoryAction||'')+'</p><div class="recommend"><b>Sizin yapacağınız:</b> '+esc(g.userAction||'Müdahale gerekmiyor.')+'</div><details class="technical"><summary>Teknik ayrıntıyı göster</summary>'+esc(i.error_text||i.result_summary||'')+'</details></div>'}
function renderOverview(){const jobs=arr(data.activeJobs),starting=arr(data.startingJobs),next=arr(data.eligibleRoadmap).slice(0,4),dep=arr(data.blockedReadyRoadmap).slice(0,4),issue=arr(data.operatorIssues)[0];let activeList=[...jobs,...starting];let activeHtml=activeList.length?activeList.map(a=>{const ri=roleInfo[a.agent_role]||[a.agent_role,a.agent_role];const startingText=a.status==='queued'?'Workflow başlatılıyor':friendlyEvent({message:a.latest_event||''});return '<div class="impact"><strong>'+esc(ri[0])+'</strong><div>'+esc(a.roadmap_title||a.job_type)+'</div><div class="muted">'+esc(a.lane_label||'')+' • '+esc(startingText)+' • '+fmt(a.updated_at)+'</div></div>'}).join(''):'<div class="empty">Şu anda aktif iş yok. Proje Direktörü düşük backlog seviyesini otomatik doldurur ve planlayıcı yeni işleri başlatır.</div>';let nextHtml=next.length?next.map(n=>'<div style="margin:8px 0">'+pill(n.status)+' <b>'+esc(n.title)+'</b><div class="muted">'+esc((roleInfo[n.agent_role]||[n.agent_role])[0])+' • bağımlılıkları tamam</div></div>').join(''):'<div class="muted">Şu anda bağımlılıkları tamamlanmış hazır iş yok.</div>';let depHtml=dep.length?'<h2 style="margin-top:16px">Bağımlılık bekleyen işler</h2>'+dep.map(n=>'<div style="margin:8px 0">'+pill(n.status)+' <b>'+esc(n.title)+'</b><div class="muted">Beklediği: '+esc(arr(n.blockedBy).map(x=>x.id+' ('+(statusTr[x.status]||x.status)+')').join(', '))+'</div></div>').join(''):'';const help=[['▶ Devam Ettir','Fabrika duraklatıldıysa yeni görev seçimini yeniden açar. Normal durumda dokunmayın.'],['↻ Planlayıcıyı Şimdi Çalıştır','Otomatik dakikalık turu beklemeden bir planlama turu çalıştırır. Teşhis dışında gerekmez.'],['＋ Projeyi Besle','Proje Direktörünü hemen çalıştırır. Sistem normalde her planlama turunda eksik çalışma hattı backlogunu otomatik tamamlar.'],['⚕ Takılan İşi Kurtar','Yalnız motor TAKILMIŞ OLABİLİR gösterirse kullanılır.'],['Ⅱ Fabrikayı Duraklat','Yeni görev başlatmayı kapatır. Bakım veya acil durdurma için kullanılır.'],['Güvenli yeniden dene','Yalnız Sorunlar/Çözümler bölümü açıkça önerirse kullanılır.']];$('#overview').innerHTML='<div class="grid"><div class="card span12"><h2>Son 6 Saat Fabrika Özeti</h2><div class="impact">'+esc(last6hSummary())+'</div></div><div class="card span8"><h2>Şu anda fabrika ne yapıyor?</h2>'+activeHtml+'<h2 style="margin-top:16px">Gerçekten çalıştırılabilir sıradaki işler</h2>'+nextHtml+depHtml+'</div><div class="card span4"><h2>Çalışma Hatları</h2><div class="lane-grid">'+arr(data.laneSummary).map(l=>{const busy=Number(l.active||0)+Number(l.starting||0);const txt=Number(l.active||0)?'çalışıyor':Number(l.starting||0)?'başlatılıyor':Number(l.eligible||0)?l.eligible+' uygun iş hazır':'Proje Direktörü backlog hazırlıyor';return '<div class="lane"><strong>'+esc(l.label)+'</strong><div class="count">'+busy+' / '+Number(l.capacity||1)+'</div><div class="muted">'+esc(txt)+'</div></div>'}).join('')+'</div></div><div class="card span6"><h2>Son sorun / müdahale önerisi</h2>'+(issue?issueHtml(issue):'<div class="empty">Müdahale gerektiren kayıt yok.</div>')+'</div><div class="card span6"><h2>Üstteki komutlar ne işe yarar?</h2><div class="help-grid">'+help.map(h=>'<div class="help"><strong>'+esc(h[0])+'</strong><div class="desc">'+esc(h[1])+'</div></div>').join('')+'</div></div></div>'}
function renderAgents(){const active=new Map(arr(data.activeJobs).map(x=>[x.agent_role,x]));$('#agents').innerHTML='<div class="qa-note"><b>Müdürler aynı sırayı beklemek zorunda değil.</b> Fabrika farklı çalışma hatlarında en fazla '+Number(data.parallelLimit||4)+' bağımsız işi aynı anda yürütür. Aynı koda bağımlı veya aynı Kod hattını kullanan işler sırayla gider.</div><div class="agent-grid">'+arr(data.agents).map(x=>{const a=active.get(x.role),ri=roleInfo[x.role]||[x.role,'Görev tanımı henüz eklenmedi.'];return '<div class="agent"><strong>'+esc(ri[0])+'</strong><span class="status '+(a?'ok':'')+'"><i class="dot"></i>'+(a?'ÇALIŞIYOR':'bekliyor')+'</span><div class="desc">'+esc(ri[1])+'</div><div class="task">'+esc(a?.roadmap_title||'Aktif görev yok')+'</div><div class="meta">'+esc(a?.lane_label||'')+(a?' • '+esc(friendlyEvent({message:a.latest_event||''}))+' • '+fmt(a.updated_at):'')+'</div></div>'}).join('')+'</div>'}
function table(rows,cols){return '<div style="overflow:auto"><table><thead><tr>'+cols.map(c=>'<th>'+esc(c[0])+'</th>').join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr>'+cols.map(c=>'<td>'+c[1](r)+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>'}
function guidanceForRoadmap(r){const found=arr(data.operatorIssues).find(x=>x.roadmap_id===r.id||x.job_id===r.work_queue_id);return found?.guidance||null}
function renderRoadmap(){$('#roadmap').innerHTML=table(arr(data.roadmap),[['No',r=>esc(r.sequence_no)],['Durum',r=>pill(r.status)+(r.eligible?'<div class="command-note">çalıştırılabilir</div>':(r.status==='ready'&&arr(r.blockedBy).length?'<div class="command-note">bağımlılık bekliyor</div>':''))],['Görev',r=>'<b>'+esc(r.title)+'</b><div class="muted">'+esc(r.id)+'</div>'],['Müdür',r=>esc((roleInfo[r.agent_role]||[r.agent_role])[0])],['Bağımlılık',r=>arr(r.blockedBy).length?arr(r.blockedBy).map(x=>esc(x.id)+' '+pill(x.status)).join('<br>'):'<span class="muted">yok / tamam</span>'],['Sonuç / Açıklama',r=>{const g=guidanceForRoadmap(r);return g?'<b>'+esc(g.title)+'</b><div class="command-note">'+esc(g.userAction)+'</div><details class="technical"><summary>Teknik sonuç</summary>'+esc(r.result_summary||'')+'</details>':esc(r.result_summary||'')}],['Güncelleme',r=>esc(fmt(r.updated_at))],['Kontrol',r=>{const g=guidanceForRoadmap(r);return g?.allowManualRetry?'<button class="btn" data-retry-roadmap="'+esc(r.id)+'">Güvenli yeniden dene</button>':(r.status==='waiting-ci'?'<span class="muted">AI QA geçti; gerçek GitHub CI bekleniyor</span>':(r.status==='waiting-human'?'<span class="muted">Gerçek CI geçti; merge onayı bekleniyor</span>':'<span class="muted">Otomatik yönetiliyor</span>'))} ]])}
function renderImpact(){const impacts=arr(data.todayImpact);$('#impact').innerHTML='<h2>Bugün Zihin Arenası için ne yaptı?</h2>'+(impacts.length?impacts.map(x=>'<div class="impact"><div>'+pill(x.status)+' <b>'+esc(x.impact_area)+'</b></div><div>'+esc(x.summary)+'</div><div class="muted">'+esc(x.repo_full_name||'')+(x.pr_url?' • <a style="color:#ffab67" target="_blank" rel="noreferrer" href="'+esc(x.pr_url)+'">Taslak PR #'+esc(x.pr_number)+'</a>':'')+' • '+fmt(x.created_at)+'</div></div>').join(''):'<div class="empty">Henüz ürüne uygulanmış/PR oluşturulmuş proje etkisi yok.</div>')}
function renderGitHub(){$('#github').innerHTML=table(arr(data.githubOperations),[['Durum',r=>pill(r.status)],['Repo',r=>esc(r.repo_full_name)],['İşlem',r=>esc(r.operation==='CREATE_PRODUCT_DRAFT_PR'?'Zihin Arenası Taslak PR oluştur':r.operation==='CREATE_DRAFT_PR'?'Fabrika Taslak PR oluştur':r.operation)],['Branch',r=>esc(r.branch_name||'')],['PR',r=>{try{const d=JSON.parse(r.data_json||'{}');return d.prUrl?'<a style="color:#ffab67" target="_blank" rel="noreferrer" href="'+esc(d.prUrl)+'">#'+esc(r.pr_number)+'</a>':esc(r.pr_number||'')}catch{return esc(r.pr_number||'')}}],['Tarih',r=>esc(fmt(r.created_at))]])}
function renderQA(){$('#qa').innerHTML='<div class="qa-note"><b>Kalite puanı proje yüzdesi değildir.</b> 60/100 → 70/100 görmeniz, yalnız o an üretilen görevin revizyonla daha iyi hale geldiğini gösterir. Asıl karar <b>GEÇTİ / REVİZYON / KARANTİNA / ENGELLENDİ</b> alanıdır. Örneğin 70 puan olup karar REVİZYON ise bazı kabul kriterleri hâlâ eksiktir ve fabrika otomatik düzeltmeye devam eder.</div>'+table(arr(data.recentReviews),[['Karar',r=>pill(r.decision)],['Kalite puanı',r=>'<b>'+esc(r.score)+'/100</b>'],['Ne anlama geliyor?',r=>'<div class="score-help">'+esc(qaMeaning(r))+'</div>'],['Kontrol eden model',r=>esc(r.reviewer_model)],['Deneme',r=>esc(r.attempt_no)],['Görev',r=>esc(r.roadmap_title||r.job_id)],['Tarih',r=>esc(fmt(r.created_at))]])}
function renderLearning(){const l=data.learning||{},fp=l.firstPass||{},d6=l.defects6h||{};const lessons=arr(l.lessons),providers=arr(l.providers);const lessonHtml=lessons.length?table(lessons,[['Öğrenilen hata',r=>'<b>'+esc(r.defect_code)+'</b>'],['Kaç kez görüldü',r=>esc(r.occurrences)],['Sonraki başarılı işler',r=>esc(r.resolved)] ]):'<div class="empty">Henüz kalıcı öğrenme kaydı oluşmadı.</div>';const providerHtml=providers.length?table(providers,[['Amaç',r=>esc(r.purpose)],['Model',r=>esc(r.model)],['Başarı',r=>esc(r.successes)],['Hata',r=>esc(r.failures)],['Ardışık hata',r=>esc(r.consecutive_failures)],['Bekleme',r=>r.cooldown_until?esc(fmt(r.cooldown_until)):'<span class="muted">yok</span>']]):'<div class="empty">Henüz model sağlık verisi oluşmadı.</div>';$('#learning').innerHTML='<div class="qa-note"><b>Bu ekran fabrikanın aynı hatayı tekrar etmemeyi öğrenip öğrenmediğini gösterir.</b> QA hataları rol ve ortak fabrika dersi olarak kalıcı hafızaya yazılır. NVIDIA model sırası sabit değildir; başarı, hata ve cooldown verisine göre dinamik seçilir.</div><div class="grid"><div class="card span3"><h2>İlk denemede kalite geçişi</h2><div class="big">'+Number(fp.rate||0)+'%</div><div class="muted">'+Number(fp.passed||0)+' / '+Number(fp.total||0)+' son 6 saat</div></div><div class="card span3"><h2>Tekrarlanan bilinen hata</h2><div class="big">'+Number(l.repeatedKnownDefects||0)+'</div><div class="muted">hedef: sürekli düşüş</div></div><div class="card span3"><h2>Son 6 saat kalite hatası</h2><div class="big">'+Number(d6.total||0)+'</div><div class="muted">benzersiz tür: '+Number(d6.unique||0)+'</div></div><div class="card span3"><h2>Öğrenme durumu</h2><div class="big">AKTİF</div><div class="muted">rol + ortak hafıza</div></div><div class="card span6"><h2>Kalıcı kalite dersleri</h2>'+lessonHtml+'</div><div class="card span6"><h2>Yapay zekâ model sağlık tablosu</h2>'+providerHtml+'</div></div>'}
function renderEvents(){$('#events').innerHTML='<div class="timeline">'+arr(data.recentEvents).map(e=>'<div class="event"><time>'+esc(fmt(e.created_at))+'</time><div><b>'+esc(eventTr[e.event_type]||e.event_type)+'</b> — '+esc(friendlyEvent(e))+'<div class="muted">'+esc(e.roadmap_title||e.job_id||'')+'</div></div></div>').join('')+'</div>'}
function renderBlockers(){const issues=arr(data.operatorIssues);const blockers=arr(data.blockers);$('#blockers').innerHTML='<div class="qa-note"><b>Buradaki ana kural:</b> “Sizin yapacağınız: Hiçbir şey” yazıyorsa butonlara dokunmayın. NVIDIA geçici hatası, stale recovery ve patch format düzeltmesi fabrika tarafından otomatik yönetilir.</div>'+(issues.length?issues.map(issueHtml).join(''):'<div class="empty">Son işlerde açıklanması gereken sorun yok.</div>')+(blockers.length?'<h2 style="margin-top:18px">Açık blocker kayıtları</h2>'+blockers.map(b=>'<div class="issue error"><h3>'+esc(b.summary)+'</h3><p>'+esc(b.details||'')+'</p><div class="muted">'+fmt(b.updated_at)+'</div></div>').join(''):'')}
$('#tabs').addEventListener('click',e=>{const b=e.target.closest('[data-tab]');if(!b)return;document.querySelectorAll('.tab').forEach(x=>x.classList.remove('on'));document.querySelectorAll('.panel').forEach(x=>x.classList.remove('on'));b.classList.add('on');$('#'+b.dataset.tab).classList.add('on')});$('#connect').onclick=async()=>{token=$('#token').value.trim();if(!token)return;sessionStorage.setItem('zf_admin_token',token);await refresh();clearInterval(timer);timer=setInterval(refresh,8000)};$('#logout').onclick=()=>{sessionStorage.removeItem('zf_admin_token');token='';showLogin()};
document.querySelector('.actions').addEventListener('click',async e=>{const b=e.target.closest('[data-action]');if(!b)return;const action=b.dataset.action;const map={start:'/admin/start',pause:'/admin/pause',cycle:'/admin/cycle',recover:'/admin/recover-stale',feed:'/admin/project-feed'};if(action==='pause'&&!confirm('Fabrikanın yeni görev başlatmasını duraklatmak istiyor musunuz?'))return;if(action==='recover'&&!confirm('Bu komut yalnız gerçekten heartbeat vermeyen takılmış işleri kurtarmak içindir. Devam edilsin mi?'))return;try{b.disabled=true;await api(map[action],{method:'POST'});toast('Komut uygulandı.');await refresh()}catch(err){toast('İşlem başarısız: '+err.message)}finally{b.disabled=false}});
$('#roadmap').addEventListener('click',async e=>{const b=e.target.closest('[data-retry-roadmap]');if(!b)return;if(!confirm('Dashboard bu görev için güvenli yeniden denemeyi öneriyor. Görev yeniden sıraya alınsın mı?'))return;try{b.disabled=true;await api('/admin/restart-roadmap-job',{method:'POST',body:JSON.stringify({roadmapId:b.dataset.retryRoadmap})});toast('Görev güvenli şekilde yeniden sıraya alındı.');await refresh()}catch(err){toast('Yeniden deneme başarısız: '+err.message)}finally{b.disabled=false}});
if(token){refresh();timer=setInterval(refresh,8000)}else showLogin();
</script></body></html>`;
}
