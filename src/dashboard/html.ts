export function dashboardHtml(): string {
  return `<!doctype html>
<html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Zihin Factory — Komuta Merkezi</title>
<style>
:root{color-scheme:dark;--bg:#0b0d10;--card:#15181d;--card2:#1b1f25;--line:#2a3038;--txt:#f5f7fa;--muted:#98a2b3;--orange:#ff7a18;--orange2:#ff9a3d;--green:#29c36a;--red:#ff5d5d;--yellow:#f6c453;--blue:#62a8ff;--violet:#aa7dff}
*{box-sizing:border-box}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:radial-gradient(circle at 20% -10%,#2a1608 0,transparent 32%),var(--bg);color:var(--txt)}button,input{font:inherit}.wrap{max-width:1500px;margin:auto;padding:22px}.top{display:flex;align-items:center;gap:16px;justify-content:space-between;position:sticky;top:0;z-index:4;background:rgba(11,13,16,.92);backdrop-filter:blur(14px);padding:14px 0;border-bottom:1px solid var(--line)}.brand{display:flex;gap:12px;align-items:center}.logo{width:42px;height:42px;border-radius:12px;background:linear-gradient(135deg,var(--orange),#ff3d00);display:grid;place-items:center;font-weight:900;font-size:21px;box-shadow:0 0 30px #ff7a1833}.brand h1{font-size:18px;margin:0}.brand small{color:var(--muted)}.actions{display:flex;gap:8px;flex-wrap:wrap}.btn{border:1px solid var(--line);background:#171a1f;color:var(--txt);padding:9px 12px;border-radius:10px;cursor:pointer}.btn:hover{border-color:#4a5360}.btn.primary{background:var(--orange);border-color:var(--orange);color:#111;font-weight:800}.btn.danger{border-color:#6a3434;color:#ffb0b0}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:14px;margin-top:16px}.card{background:linear-gradient(180deg,#171a1f,#121519);border:1px solid var(--line);border-radius:15px;padding:15px;min-width:0}.span3{grid-column:span 3}.span4{grid-column:span 4}.span5{grid-column:span 5}.span6{grid-column:span 6}.span7{grid-column:span 7}.span8{grid-column:span 8}.span12{grid-column:span 12}.card h2{font-size:14px;margin:0 0 12px;color:#d9dee7}.big{font-size:30px;font-weight:850;letter-spacing:-1px}.muted{color:var(--muted);font-size:12px}.status{display:inline-flex;gap:6px;align-items:center;border:1px solid var(--line);border-radius:999px;padding:4px 8px;font-size:11px}.dot{width:7px;height:7px;border-radius:50%;background:var(--muted)}.ok .dot{background:var(--green);box-shadow:0 0 8px var(--green)}.warn .dot{background:var(--yellow)}.bad .dot{background:var(--red)}.metric{display:flex;justify-content:space-between;align-items:end;gap:12px}.bar{height:7px;background:#252a31;border-radius:99px;overflow:hidden;margin-top:10px}.bar>i{height:100%;display:block;background:linear-gradient(90deg,var(--orange),var(--orange2));border-radius:99px}.tabs{display:flex;gap:8px;overflow:auto;padding:2px 0 8px}.tab{white-space:nowrap;background:#14171b;border:1px solid var(--line);color:#c8cfd9;padding:8px 11px;border-radius:9px;cursor:pointer}.tab.on{border-color:var(--orange);color:#fff;background:#27170c}.panel{display:none}.panel.on{display:block}.table{width:100%;border-collapse:collapse;font-size:12px}.table th{text-align:left;color:#8f99a8;font-weight:600;padding:9px;border-bottom:1px solid var(--line)}.table td{padding:9px;border-bottom:1px solid #21262d;vertical-align:top;word-break:break-word}.pill{padding:3px 7px;border-radius:999px;font-size:10px;border:1px solid var(--line);display:inline-block}.pill.done,.pill.completed,.pill.merged,.pill.PASS{color:#8cf0b1;border-color:#285e3d}.pill.running,.pill.dispatched{color:#8fc4ff;border-color:#315a86}.pill.ready{color:#e1e5eb}.pill.quarantine,.pill.RETRY,.pill.waiting-human{color:#ffd987;border-color:#765d23}.pill.blocked,.pill.failed,.pill.closed-unmerged{color:#ff9e9e;border-color:#713838}.timeline{display:grid;gap:8px}.event{display:grid;grid-template-columns:155px 1fr;gap:10px;padding:8px 0;border-bottom:1px solid #22272e;font-size:12px}.event time{color:#7f8997}.agent-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(250px,1fr));gap:10px}.agent{background:#111419;border:1px solid var(--line);padding:12px;border-radius:12px}.agent strong{display:block;margin-bottom:5px}.agent .task{font-size:12px;color:#cbd2dc;min-height:32px}.agent .meta{font-size:11px;color:var(--muted);margin-top:8px}.impact{border-left:3px solid var(--orange);padding:9px 11px;background:#121519;border-radius:7px;margin:8px 0}.impact a{color:#ffab67}.empty{color:var(--muted);padding:18px 0;text-align:center}.login{position:fixed;inset:0;z-index:20;background:#080a0dcc;display:grid;place-items:center;backdrop-filter:blur(8px)}.login.hidden{display:none}.loginbox{width:min(460px,92vw);background:#14171c;border:1px solid #323841;border-radius:18px;padding:22px;box-shadow:0 30px 90px #000}.loginbox h2{margin-top:0}.loginbox input{width:100%;background:#090b0e;border:1px solid #343a44;color:#fff;border-radius:10px;padding:12px;margin:8px 0 12px}.toast{position:fixed;right:20px;bottom:20px;background:#171b20;border:1px solid var(--line);padding:12px 14px;border-radius:10px;display:none;z-index:30}.toast.show{display:block}.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;color:var(--orange2)}@media(max-width:900px){.span3,.span4,.span5,.span6,.span7,.span8{grid-column:span 12}.top{align-items:flex-start;flex-direction:column}.event{grid-template-columns:1fr}.wrap{padding:12px}.actions{width:100%}}
</style></head><body>
<div id="login" class="login"><div class="loginbox"><div class="kicker">Güvenli Yönetici Girişi</div><h2>Zihin Factory Komuta Merkezi</h2><p class="muted">FACTORY_ADMIN_TOKEN yalnız bu tarayıcı sekmesinin sessionStorage alanında tutulur; sunucuya Bearer header olarak gönderilir.</p><input id="token" type="password" autocomplete="off" placeholder="FACTORY_ADMIN_TOKEN"><button id="connect" class="btn primary">Dashboard'a Bağlan</button><div id="loginerr" class="muted" style="margin-top:10px"></div></div></div>
<div class="wrap"><header class="top"><div class="brand"><div class="logo">ZF</div><div><h1>Zihin Factory — Komuta Merkezi</h1><small id="subtitle">Dashboard bağlanıyor…</small></div></div><div class="actions"><button class="btn primary" data-action="start">▶ Resume</button><button class="btn" data-action="cycle">↻ Governor Now</button><button class="btn" data-action="feed">＋ Project Feed</button><button class="btn" data-action="recover">⚕ Recover</button><button class="btn danger" data-action="pause">Ⅱ Pause</button><button class="btn" id="logout">Token Çıkış</button></div></header>
<section class="grid" id="metrics"></section>
<section class="grid"><div class="card span12"><div class="tabs" id="tabs"><button class="tab on" data-tab="overview">Genel Bakış</button><button class="tab" data-tab="agents">Müdürler / Ajanlar</button><button class="tab" data-tab="roadmap">Canlı İş Akışı</button><button class="tab" data-tab="impact">Zihin Arenası Etkisi</button><button class="tab" data-tab="github">GitHub / PR</button><button class="tab" data-tab="qa">QA</button><button class="tab" data-tab="events">Canlı Olaylar</button><button class="tab" data-tab="blockers">Blocker</button></div>
<div id="overview" class="panel on"></div><div id="agents" class="panel"></div><div id="roadmap" class="panel"></div><div id="impact" class="panel"></div><div id="github" class="panel"></div><div id="qa" class="panel"></div><div id="events" class="panel"></div><div id="blockers" class="panel"></div></div></section></div><div id="toast" class="toast"></div>
<script>
const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let token=sessionStorage.getItem('zf_admin_token')||'', data=null, timer=null;
function pill(v){return '<span class="pill '+esc(v)+'">'+esc(v||'-')+'</span>'}function arr(v){return Array.isArray(v)?v:[]}function state(k){return data?.stateMap?.[k]??null}function fmt(t){if(!t)return'-';try{return new Date(t.replace(' ','T')+'Z').toLocaleString('tr-TR')}catch{return t}}
async function api(path,opt={}){const h={'Authorization':'Bearer '+token,...(opt.headers||{})};if(opt.body&&!h['Content-Type'])h['Content-Type']='application/json';const r=await fetch(path,{...opt,headers:h});const j=await r.json().catch(()=>({}));if(r.status===401){showLogin('Token geçersiz veya süresi dolmuş.');throw new Error('unauthorized')}if(!r.ok||j.ok===false)throw new Error(j.error||('HTTP '+r.status));return j}
function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),3500)}
function showLogin(msg=''){clearInterval(timer);$('#login').classList.remove('hidden');$('#loginerr').textContent=msg}function hideLogin(){$('#login').classList.add('hidden')}
async function refresh(){try{data=await api('/dashboard/api');render();hideLogin()}catch(e){if(String(e.message)!=='unauthorized')toast('Dashboard: '+e.message)}}
function motorState(){const on=state('continuous_enabled')==='1', jobs=arr(data.activeJobs);if(!on)return{label:'DURAKLATILDI',cls:'warn',detail:'continuous_enabled=0'};if(!jobs.length)return{label:'BEKLİYOR',cls:'ok',detail:state('last_governor_action')||'Governor sıradaki işi seçiyor'};const a=jobs[0], raw=String(a.updated_at||'').replace(' ','T')+'Z', age=Math.max(0,(Date.now()-Date.parse(raw))/60000);if(Number.isFinite(age)&&age>6)return{label:'STALLED',cls:'bad',detail:(a.latest_event||a.job_type||'aktif iş')+' • '+Math.round(age)+' dk heartbeat yok'};return{label:'ÇALIŞIYOR',cls:'ok',detail:a.latest_event||a.job_type||'aktif workflow'}}
function render(){const on=state('continuous_enabled')==='1', pct=Number(state('project_completion_percent')||82), active=arr(data.activeJobs).length, waiting=arr(data.impact).filter(x=>x.status==='waiting-human').length, today=Number(data.today?.completed||0)+Number(data.today?.merged||0), motor=motorState();$('#subtitle').textContent='v'+data.version+' • '+motor.label+' • son yenileme '+new Date().toLocaleTimeString('tr-TR');
$('#metrics').innerHTML=\`<div class="card span3"><div class="kicker">Canlı Motor Çalışması</div><div class="big">\${motor.label}</div><span class="status \${motor.cls}"><i class="dot"></i>\${esc(motor.detail)}</span></div><div class="card span3"><div class="kicker">Zihin Arenası</div><div class="metric"><div><div class="big">\${pct}%</div><div class="muted">raporlanan genel baseline</div></div></div><div class="bar"><i style="width:\${Math.min(100,pct)}%"></i></div></div><div class="card span3"><div class="kicker">Aktif İş</div><div class="big">\${active}</div><div class="muted">waiting human: \${waiting}</div></div><div class="card span3"><div class="kicker">Bugünkü Etki</div><div class="big">\${today}</div><div class="muted">tamamlanan/merge edilen proje etkisi</div></div>\`;
renderOverview();renderAgents();renderRoadmap();renderImpact();renderGitHub();renderQA();renderEvents();renderBlockers()}
function renderOverview(){const a=arr(data.activeJobs)[0], next=arr(data.roadmap).find(x=>x.status==='ready');$('#overview').innerHTML=\`<div class="grid"><div class="card span6"><h2>Şu an fabrika ne yapıyor?</h2>\${a?\`<div class="impact"><strong>\${esc(a.agent_role||a.job_type)}</strong><div>\${esc(a.roadmap_title||a.job_type)}</div><div class="muted">\${esc(a.latest_event||'Çalışıyor')} • \${fmt(a.updated_at)}</div></div>\`:'<div class="empty">Aktif workflow yok. Governor sıradaki uygun işi seçer.</div>'}<h2 style="margin-top:15px">Sıradaki uygun iş</h2>\${next?\`<div>\${pill(next.status)} <b>\${esc(next.title)}</b><div class="muted">\${esc(next.agent_role)}</div></div>\`:'<div class="muted">Hazır iş yok veya dependency / insan merge kapısı bekleniyor.</div>'}</div><div class="card span6"><h2>8. Sınıf Türkçe</h2><div class="big">\${esc(state('g8_turkish_verified_integration')||'7/10')}</div><div class="muted">Verified native integration • Research \${esc(state('g8_turkish_research')||'10/10')} • live \${esc(state('g8_turkish_live')||'-')}</div><h2 style="margin-top:18px">Project Feeder</h2><span class="status \${state('project_feeder_enabled')==='1'?'ok':'warn'}"><i class="dot"></i>\${esc(state('last_project_feeder_action')||'hazır')}</span><div class="muted" style="margin-top:7px">\${fmt(state('last_project_feeder_at'))}</div></div></div>\`}
function renderAgents(){const active=new Map(arr(data.activeJobs).map(x=>[x.agent_role,x]));$('#agents').innerHTML='<div class="agent-grid">'+arr(data.agents).map(x=>{const a=active.get(x.role);return \`<div class="agent"><strong>\${esc(x.role)}</strong><span class="status \${a?'ok':''}"><i class="dot"></i>\${a?'ÇALIŞIYOR':'bekliyor'}</span><div class="task">\${esc(a?.roadmap_title||'Aktif görev yok')}</div><div class="meta">\${esc(a?.latest_event||x.provider||'')} \${a?'• '+fmt(a.updated_at):''}</div></div>\`}).join('')+'</div>'}
function table(rows,cols){if(!rows.length)return'<div class="empty">Kayıt yok.</div>';return '<div style="overflow:auto"><table class="table"><thead><tr>'+cols.map(c=>'<th>'+esc(c[0])+'</th>').join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr>'+cols.map(c=>'<td>'+c[1](r)+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>'}
function renderRoadmap(){$('#roadmap').innerHTML=table(arr(data.roadmap),[['No',r=>esc(r.sequence_no)],['Durum',r=>pill(r.status)],['Görev',r=>'<b>'+esc(r.title)+'</b><div class="muted">'+esc(r.id)+'</div>'],['Müdür',r=>esc(r.agent_role)],['Sonuç',r=>esc(r.result_summary||'')],['Güncelleme',r=>esc(fmt(r.updated_at))],['Kontrol',r=>(r.status==='quarantine'||r.status==='blocked')?'<button class="btn" data-retry-roadmap="'+esc(r.id)+'">Retry</button>':r.status==='waiting-human'?'<span class="muted">PR merge bekliyor</span>':'']])}
function renderImpact(){const impacts=arr(data.todayImpact);$('#impact').innerHTML='<h2>Bugün Zihin Arenası için ne yaptı?</h2>'+(impacts.length?impacts.map(x=>\`<div class="impact"><div>\${pill(x.status)} <b>\${esc(x.impact_area)}</b></div><div>\${esc(x.summary)}</div><div class="muted">\${esc(x.repo_full_name||'')} \${x.pr_url?' • <a target="_blank" rel="noreferrer" href="'+esc(x.pr_url)+'">PR #'+esc(x.pr_number)+'</a>':''} • '+fmt(x.created_at)+'</div></div>\`).join(''):'<div class="empty">Henüz kayıtlı proje etkisi yok. İlk repo keşfi/patch sonucu burada görünecek.</div>')}
function renderGitHub(){$('#github').innerHTML=table(arr(data.githubOperations),[['Durum',r=>pill(r.status)],['Repo',r=>esc(r.repo_full_name)],['İşlem',r=>esc(r.operation)],['Branch',r=>esc(r.branch_name||'')],['PR',r=>{try{const d=JSON.parse(r.data_json||'{}');return d.prUrl?'<a style="color:#ffab67" target="_blank" rel="noreferrer" href="'+esc(d.prUrl)+'">#'+esc(r.pr_number)+'</a>':esc(r.pr_number||'')}catch{return esc(r.pr_number||'')}}],['Tarih',r=>esc(fmt(r.created_at))]])}
function renderQA(){$('#qa').innerHTML=table(arr(data.recentReviews),[['Karar',r=>pill(r.decision)],['Puan',r=>'<b>'+esc(r.score)+'/100</b>'],['Reviewer',r=>esc(r.reviewer_model)],['Attempt',r=>esc(r.attempt_no)],['Görev',r=>esc(r.roadmap_title||r.job_id)],['Tarih',r=>esc(fmt(r.created_at))]])}
function renderEvents(){$('#events').innerHTML='<div class="timeline">'+arr(data.recentEvents).map(e=>\`<div class="event"><time>\${esc(fmt(e.created_at))}</time><div><b>\${esc(e.event_type)}</b> — \${esc(e.message)}<div class="muted">\${esc(e.roadmap_title||e.job_id||'')}</div></div></div>\`).join('')+'</div>'}
function renderBlockers(){$('#blockers').innerHTML=arr(data.blockers).length?arr(data.blockers).map(b=>\`<div class="impact" style="border-left-color:var(--red)">\${pill(b.severity)} <b>\${esc(b.summary)}</b><div>\${esc(b.details||'')}</div><div class="muted">\${fmt(b.updated_at)}</div></div>\`).join(''):'<div class="empty">Açık blocker yok.</div>'}
$('#tabs').addEventListener('click',e=>{const b=e.target.closest('[data-tab]');if(!b)return;document.querySelectorAll('.tab').forEach(x=>x.classList.remove('on'));document.querySelectorAll('.panel').forEach(x=>x.classList.remove('on'));b.classList.add('on');$('#'+b.dataset.tab).classList.add('on')});
$('#connect').onclick=async()=>{token=$('#token').value.trim();if(!token)return;sessionStorage.setItem('zf_admin_token',token);await refresh();clearInterval(timer);timer=setInterval(refresh,10000)};$('#logout').onclick=()=>{sessionStorage.removeItem('zf_admin_token');token='';showLogin()};
document.querySelector('.actions').addEventListener('click',async e=>{const b=e.target.closest('[data-action]');if(!b)return;const map={start:'/admin/start',pause:'/admin/pause',cycle:'/admin/cycle',recover:'/admin/recover-stale',feed:'/admin/project-feed'};try{b.disabled=true;const r=await api(map[b.dataset.action],{method:'POST'});toast(b.dataset.action+' OK');await refresh()}catch(err){toast('İşlem başarısız: '+err.message)}finally{b.disabled=false}});
$('#roadmap').addEventListener('click',async e=>{const b=e.target.closest('[data-retry-roadmap]');if(!b)return;try{b.disabled=true;await api('/admin/restart-roadmap-job',{method:'POST',body:JSON.stringify({roadmapId:b.dataset.retryRoadmap})});toast('Roadmap retry başlatıldı');await refresh()}catch(err){toast('Retry başarısız: '+err.message)}finally{b.disabled=false}});
if(token){refresh();timer=setInterval(refresh,10000)}else showLogin();
</script></body></html>`;
}
